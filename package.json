{
  "name": "hono-terraform",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "hono": "^4.6.20"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
